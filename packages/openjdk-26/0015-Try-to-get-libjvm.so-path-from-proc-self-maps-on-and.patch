From rebased@local Fri Apr 18 22:00:00 2026
Subject: [PATCH] Try to get libjvm.so path from /proc/self/maps on android

Rebased for jdk26: os::jvm_path was moved from os_linux.cpp to
os_posix.cpp upstream, so the Android-specific path resolution now
lives in os_posix.cpp.

--- a/src/hotspot/os/posix/os_posix.cpp
+++ b/src/hotspot/os/posix/os_posix.cpp
@@ -1093,6 +1093,32 @@
 
 static char saved_jvm_path[MAXPATHLEN] = {0};
 
+#ifdef __ANDROID__
+static bool read_so_path_from_maps(const char* so_name, char* buf, int buflen) {
+  FILE *fp = fopen("/proc/self/maps", "r");
+  assert(fp, "Failed to open /proc/self/maps");
+  if (!fp) {
+    return false;
+  }
+
+  char maps_buffer[2048];
+  while (fgets(maps_buffer, 2048, fp) != NULL) {
+    if (strstr(maps_buffer, so_name) == NULL) {
+      continue;
+    }
+
+    char *so_path = strchr(maps_buffer, '/');
+    so_path[strlen(so_path) - 1] = '\0'; // Cut trailing \n
+    jio_snprintf(buf, buflen, "%s", so_path);
+    fclose(fp);
+    return true;
+  }
+
+  fclose(fp);
+  return false;
+}
+#endif
+
 // Find the full path to the current module, libjvm.so
 void os::jvm_path(char *buf, jint buflen) {
   // Error checking.
@@ -1128,6 +1154,22 @@
   }
   fname = dli_fname;
 #endif // AIX
+#ifdef __ANDROID__
+  if (fname[0] == '\0') {
+    return;
+  }
+
+  if (strchr(fname, '/') == NULL) {
+    bool ok = read_so_path_from_maps(fname, buf, buflen);
+    assert(ok, "unable to turn relative libjvm.so path into absolute");
+    (void)ok;
+  } else {
+    jio_snprintf(buf, buflen, "%s", fname);
+  }
+  strncpy(saved_jvm_path, buf, MAXPATHLEN);
+  saved_jvm_path[MAXPATHLEN - 1] = '\0';
+  return;
+#endif
   char* rp = nullptr;
   if (fname[0] != '\0') {
     rp = os::realpath(fname, buf, buflen);
